# Stages governed by audit granularity mode (full / story / epic).
# Referenced by scripts/runtime-governance.ts
version: "1.0"
granularity_governed_stages:
  - story_create
  - story_audit
  - specify
  - plan
  - gaps
  - tasks
  - implement
