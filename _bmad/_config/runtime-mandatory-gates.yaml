# Runtime mandatory gates: (flow, stage) pairs where mandatoryGate must be true.
# Referenced by scripts/runtime-governance.ts; rule id appears in policy.reason when hit.
version: "1.0"
gates:
  - id: mandatory_post_audit_story
    flow: story
    stage: post_audit
  - id: mandatory_post_audit_bugfix
    flow: bugfix
    stage: post_audit
