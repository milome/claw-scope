# Tasks 阶段（tasks.md）

**对应 speckit-workflow skill §4**

- 对照需求、plan、GAPS 生成 tasks.md，含需求映射与验收模板。
- 必须含集成/端到端测试任务。
- 审计闭环：code-review，audit-prompts.md §4；tasks≥10 或跨 artifact 时执行 analyze。

*详细步骤请从 Cursor 全局 skill `speckit-workflow` 的 §4 节补齐。*
